//15) Ler um valor e escrever se é positivo ou negativo (considere o valor zero como positivo). 
function executar15(){
    //entrada de dados
    let numero = parseInt(prompt("Digite um número: "))

    //processamento
    if(numero<0){
        //saida de dados
       alert("O número é negativo!")
    }
    else{
        //saida de dados
        alert("O número pe positivo!")
    }
}
